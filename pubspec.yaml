name: stress_detection
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_nav_bar: ^5.0.6
  fl_chart: ^0.66.0
  percent_indicator: ^4.2.3
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  google_fonts: ^6.2.1

  # Firebase Dependencies (Only Auth and Realtime Database)
  firebase_core: ^3.11.0
  firebase_auth: ^5.5.1
  firebase_database: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
